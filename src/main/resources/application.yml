spring:
  datasource:
    url: jdbc:mysql://localhost:3306/local_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092

postgres:
  datasource:
    url: jdbc:postgresql://localhost:5432/local_db
    username: postgres
    password: root
